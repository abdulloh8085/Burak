            <div>
                <%= member.memberNick %>
            </div>
            <% products.map( function(value, key) { %>
                <p>
                    <%= value.productName %> - <%= value.productSize %>
                </p>

                <% } )%>
        </div>

    </body>